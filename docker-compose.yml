services:
  microservice_react_frontend:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: microservice_react_frontend
    ports:
      - "8000:80"
    env_file:
      - .env
